using System;
using System.Collections;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace BankSystem
{
    internal class EmployeeModel : CustomerModel
    {
        /*
        private List<CustomerModel> userList { get; set; } = new List<CustomerModel>();
        public EmployeeModel() : base() { }
        public EmployeeModel(string ssn, string username, string password, string phone, string address)
        : base(ssn, username, password, phone, address) { }
        public void addCustomer(CustomerModel user)
        {
            userList.Add(user);
        }
        */
    }
}
